# Coding Standards

- Prefer small, focused functions.
- Use `const` unless reassignment is required.
- Add explicit return types to exported functions.
- Avoid deeply nested conditional statements.

# Back-End Standards
 - Use lamda expression to reduce number of lines of code 
 - 

# Front-End Standards
 - use React 18 latest features to reduce redendency
 